#include<stdio.h>
#include<conio.h>
int main(){
	int a;
	printf("the size of a is%d\n",sizeof(a));
	char b;
	printf("the size of a is%d\n",sizeof(b));
	float c;
	printf("the size of a is%d\n",sizeof(c));
	double d;
	printf("the size of a is%d\n",sizeof(d));
return 0;	
}

